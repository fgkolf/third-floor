---
import Map from "./Map.astro";

const contactDetails = {
  phones: ["6981231234", "2131234567"],
  address: "Σύρου 19 & Ελ. Βενιζέλου, Γαλάτσι 111 47",
  workingHours: [
    { day: "Δευτέρα", hours: "Closed" },
    { day: "Τρίτη - Παρ", hours: "10:00 - 19:00" },
    { day: "Σάββατο", hours: "10:00 - 17:00" },
    { day: "Κυριακή", hours: "Closed" },
  ],
  social: [
    { name: "i: @3rdfloor", url: "https://www.instagram.com/yourprofile" },
    { name: "f: @3rdfloor", url: "https://www.facebook.com/yourprofile" },
  ],
};

const contactDetailClass =
  "p-4 bg-[var(--bg-color)]/70 rounded-lg shadow-md flex flex-col gap-4 items-center";
---

<div class="contact">
  <div
    class="p-[8%] flex flex-col xl:flex-row gap-12 items-center justify-between"
  >
    <div class="max-sm:w-[70%] grid grid-cols-1 sm:grid-cols-2 gap-12">
      <div class={`${contactDetailClass}`}>
        <img class="w-6" src="/images/contact/phone.png" alt="phone" />
        <div>
          {
            contactDetails.phones.map((phone) => (
              <div>
                <a class="sm:hidden" href={`tel:${phone}`}>
                  {phone}
                </a>
                <p class="max-sm:hidden">{phone}</p>
              </div>
            ))
          }
        </div>
      </div>
      <div class={`${contactDetailClass}`}>
        <img class="w-6" src="/images/contact/location.png" alt="address" />
        <p>{contactDetails.address}</p>
      </div>
      <div class={`${contactDetailClass}`}>
        <img class="w-6" src="/images/contact/clock.png" alt="working hours" />
        <ul>
          {
            contactDetails.workingHours.map(({ day, hours }) => (
              <li>
                {day}: {hours}
              </li>
            ))
          }
        </ul>
      </div>
      <div class={`${contactDetailClass}`}>
        <img
          class="w-6"
          src="/images/contact/social.png"
          alt="social media links"
        />
        <div>
          {
            contactDetails.social.map(({ name, url }) => (
              <a
                class="block"
                href={url}
                target="_blank"
                rel="noopener noreferrer"
              >
                {name}
              </a>
            ))
          }
        </div>
      </div>
    </div>
    <Map />
  </div>
</div>
